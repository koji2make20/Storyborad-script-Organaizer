import type {Metadata} from 'next';import './globals.css';export const metadata:Metadata={title:'Storyboard Script Organizer Web',description:'脚本を整理し、カットごとの尺を計算・書き出しできるWebアプリ'};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="ja"><body>{children}</body></html>}
